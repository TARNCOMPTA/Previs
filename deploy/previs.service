# Unité systemd — alternative à Docker, pour une installation directe sur le VPS.
# Copier dans /etc/systemd/system/previs.service, puis :
#   sudo systemctl daemon-reload && sudo systemctl enable --now previs

[Unit]
Description=Previs — logiciel de prévisionnel financier TARN COMPTA
Documentation=file:///opt/previs/deploy/README-deploiement.md
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=previs
Group=previs
WorkingDirectory=/opt/previs
EnvironmentFile=/opt/previs/.env
Environment=NODE_ENV=production
# ProtectSystem=strict rend /opt/previs en lecture seule et ProtectHome=true vide /home
# et /root : sans cette ligne, le service tourne sans aucun foyer inscriptible. Chromium
# s'en est passé, mais un service dans cet état est un piège en attente.
Environment=HOME=/opt/previs/data/foyer
Environment=XDG_CONFIG_HOME=/opt/previs/data/foyer/config
Environment=XDG_CACHE_HOME=/opt/previs/data/foyer/cache
ExecStart=/usr/bin/node packages/server/dist/index.js
Restart=on-failure
RestartSec=5
KillSignal=SIGTERM
TimeoutStopSec=30

# Cloisonnement : le service n'a besoin d'écrire que dans son répertoire de données.
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/previs/data
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
LockPersonality=true

StandardOutput=journal
StandardError=journal
SyslogIdentifier=previs

[Install]
WantedBy=multi-user.target
